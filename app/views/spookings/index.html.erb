<%= render 'shared/blood',title: "Avaiable killers!" %>



<div class="card-container">
  <% @spooking.each do | spooking| %>
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><strong><%= spooking.start_date %></strong></h5>
          <p class="card-text"><%= spooking.end_date %> </p>
            <%= link_to "View spooking", spooking_path(spooking), class: "btn btn-danger" %>

        </div>
      </div>
  <% end %>
</div>

  <%= link_to root_path do %>
    <h1 class="glitch" data-text="back">
      <%= link_to "back", root_path %>
      </h1>

    <% end %>
